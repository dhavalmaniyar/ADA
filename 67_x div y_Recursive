#include<iostream>
using namespace std;
int divide(int x,int y){
    if(x<y){

        return 1+divide(x-y,y);
    }
}
int main()
{
    int x,y;
    cout<<"Enter x and y "<<endl;
    cin>>x>>y;
    cout<<"Result = "<<divide(x,y);
    return 0;
}
